import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingVertical: 50,
    alignContent: 'center',
    backgroundColor: '#111121',
  },
  flatListStyle: {
    width: "90%",
    alignSelf: "center",
  }  
})
